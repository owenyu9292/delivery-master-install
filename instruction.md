# 현재 프로젝트 기준

적용일: 2026-09-09. 사용자 지시: 기존 프로젝트 강제 규칙은 모두 해제하고, 인스톨판만 수정하는 기준을 유지한다. 나머지는 코기가 판단한다.

## 유지하는 기준

수정 대상은 `C:\Codex55Workspace\delivery-master\delivery-master-install-deploy`의 Android 인스톨 앱이다. 기존 PWA 게시본, 상위 폴더의 PWA v1, season2는 보관용 원본이며 수정하거나 배포하지 않는다.

## 작업 방식

프로젝트 자체의 반복 승인 대기, 고정 보고 양식, 문서 갱신 의무, 고정 워커·모델, 고정 검수 절차, 커밋 훅 강제는 해제한다. 코기가 현재 요청과 위험도에 맞춰 구현·검수·기록 방법을 판단한다. 이 변경 자체가 앱 변경·커밋·푸시·배포 실행 요청은 아니다.

과거 문서에 남은 승인·절차·배포·모델 규정은 현재 강제 규칙이 아니다. 기능 요구사항, 데이터 의미, 기존 작업 결과는 참고 정보로 보존한다. 이전 규칙 원문은 `docs/history/2026-09-09-rules-retired/`에 보관한다.

Codex 자체의 상위 안전·권한 제한은 이 프로젝트 문서로 해제하지 않는다.

## 테스트 파일 정리

2026-09-09 사용자 지시: 이후 검수에서 생성한 앱 저장·내보내기용 테스트 JSON 파일은 검수 후 삭제한다. 테스트가 만든 파일의 경로를 구분해 기록하고 그 파일만 정리한다. 실제 업무 기록, 사용자가 제공한 JSON, 복구용 원본 백업은 삭제 대상이 아니다. 개인 자료를 포함하지 않는 검사 결과 요약은 검수 증거로 구분한다. 이번 테스트 JSON은 사용자가 직접 삭제했으므로 다시 삭제 작업을 하지 않는다.
