# 리포트 화면 재설계 및 정정 재검수

## 변경과 상태

- 2026-09-09 사용자 승인: DOS창처럼 보이는 작은 리포트 칸을 읽기 편하게 개선. 인스톨판만 수정, PWA와 실제 폰 자료 변경 없음.
- 날짜/완료 상태, 총수량/배송시간/효율, 업무 시간 흐름, 구역별 내역, 미주 A/B, 도우미, 별도 작업·이벤트, 확인 필요 순으로 구성했다.
- 리포트 탭과 통계 날짜 조회가 같은 렌더러를 쓴다. 260px 제한과 내부 스크롤을 없애고 문서 전체를 페이지 스크롤로 읽는다. 복사 버튼은 상단 아이콘과 최하단에 둔다.
- 탁색 블루/중립색과 시스템 글꼴 유지. 수량이 아닌 효율 미확정은 숫자를 만들어내지 않고 미확정으로 표시한다.
- Python은 사용하지 않는다. DayRecord.timeline에서 TypeScript calculateDay 결과를 만들고 화면/복사 리포트를 파생한다. 리포트 표시나 복사 자체는 자료를 저장하지 않는다.
- 기존 복사 텍스트 형식과 계산 엔진 유지. 단, 아래 검수에서 발견한 미주 상세 정정의 공통 저장 로직을 보강했다.
- 커밋/푸시/새 공개/폰 설치 없음. v38 번호는 아직 변경하지 않았다. 현재 검사용 APK를 새 배포판으로 안내하지 않는다.

## 검수 중 실제 발견한 결함

- aTotal40/restTotal60만 있는 미주 기록을 로그에서 총량100->120으로 정정하면 B60이 그대로 남았다. bTotal 필드가 있어야 정정하는 기존 조건 때문이었다.
- zoneEdit 공통 경로에서 bTotal/restTotal/mijuRest 및 동별 합계만 존재하는 형식을 인식하도록 수정했다. 알려진 A는 보존하고 변경된 구역 총량에서 B를 구하며 기존 나머지 필드도 함께 갱신한다.
- 5가지 필드 형식 x4회 수량 왕복 및 JSON 재개방 검사를 추가했다. 미주 외 구역에 A/B를 붙이지 않는 기존 검사도 통과.
- 브라우저에서 실제 로그 수량100->120 저장 후 A40/B80, 앱 reload 후120, 통계 날짜 리포트120을 확인했다. 과거 자료를 일괄 덮어쓰지는 않았다.

## 최종 검수 결과

- npm run check 통과: 64/64 도메인, 플랫폼/SQLite/복구, 시간2684조합, 자동정리18, 시간연결17묶음, 도우미11, 통계15묶음+600변형, 뒤로가기5묶음 등.
- npm run check:browser 통과: 업무·시간·통계·스크롤·리포트38묶음 + 저장·정정22묶음. 원래 테스트의 계산 기대값을 유지하고 새 구조의 실제 표시값으로 대조했다.
- 리포트: 411/360폭에서 내부 스크롤 없음, 실제 wheel로 최하단 접근 후 복사 버튼 좌표 클릭, 원본 불변, 미주100/A40/B60와 복사 텍스트 일치.
- 19개 구역, 매우 긴 이름과 HTML 모양 문자열, 150% 글자, 360폭에서 가로 잘림/삽입된 HTML 없음 및 최하단 접근 확인. 빈 업무는0/미확정이고 원본 불변.
- 이전 스크롤60조합, 저장 실패/중복 탭/취소/중단/수량 초안 복구, 순서/시간/도우미 정정 검수도 최종 소스에서 재통과.
- 실제 SQLite 파일 재개방/원자적 실패6종: 임시 delivery-sqlite-safety-8KNPSl. Android 플러그인 실기기 검사가 아니다.
- build, cap sync android, assembleDebug 성공. APK 내장 app.js/field-app.css/styles.css/index.html과 dist 해시 일치.
- 최종 검사용 APK SHA256: 15F0A4200885017B8E3EFDBE51885857A14463C75DD85D073A3870E751CFE58C.
- 기존 서명 SHA256 유지: f7f90c90211f12d16e03ce0381657690b5bd7de06b52f46abad70a795081f164.

## 증거와 한계

- docs/review-report-2026-09-09/field-result.json (38묶음), storage-result.json (22묶음).
- 같은 폴더 17-report-411.png 및18-report-bottom-360.png는 직접 시각 확인했다. 다른 폭/큰 글자19개 구역 스크린샷도 보관한다. 합성 자료이지 사용자의 실제 하루 기록이 아니다.
- 원본 임시 결과: C:\Users\Lenovo\AppData\Local\Temp\delivery-field-v37-PZT0Vc, delivery-handling-03fY72.
- 최초 신규 테스트의 저장 전 JS 객체와 JSON의 undefined 속성 차이 비교 오류는 실제 DB 원본을 비교하도록 수정했다. 이후 실제 B 정정 결함은 기대값을 완화하지 않고 코드 수정 후 재검수했다.
- Fold7 실기기 미연결. 실제 유료 폰트/삼성 키보드/OS 뒤로가기/SQLite·파일 왕복은 미확인이다. 브라우저와 APK 빌드 성공을 실기기 통과로 표현하지 않는다.
- 공개 앱은 여전히 기존 v38이다. 배포 지시 후 버전 갱신과 최종 패키징/해시/업로드 확인이 필요하다.
