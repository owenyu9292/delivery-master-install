# 2026-06-07 source-of-truth 이관 정리

- 핫픽스 채널은 일반 작업창을 대체하는 개발 채널이 아니라 핸드폰 현장 접속 안정성을 위한 초경량 장애 대응 채널로 정리했다.
- `instruction.md`에 `0-8. 최상위 작업 전제: 핫픽스 채널은 현장 접속용 초경량 채널`을 추가했다.
- `HOTFIX_HANDOFF.md`에도 같은 운영 목적을 넣어, 새 핫픽스 창이 긴 대화 없이 최신 상태만 읽고 움직이게 했다.
- 핫픽스 채널에서 현장 긴급 수정 요청이 들어오면 현장에서 막힌 부분을 임시로 틀어막는 최소 수정, 검사, 빌드, 배포까지 전량 승인으로 간주하고 승인 질문 없이 즉시 처리한다는 규칙을 추가했다.
- 핫픽스는 정식 설계 완료가 아니므로, 막은 내용과 남은 정식 수정 항목을 문서화하고 일반 작업창으로 넘긴다는 기준을 추가했다.
- `C:\Codex55Workspace\delivery-master\delivery-master-install-deploy`를 개발앱 PWA의 단일 source-of-truth 및 GitHub Pages 배포 repo로 고정했다.
- 이전 작업 폴더 `C:\#WORKSPACE\AI_HUB\AI_WORKSPACE\PROJECTS\delivery-master-install`에서 `src`, `scripts`, `test`, `public`, 빌드 설정, 작업 문서를 이관했다.
- `current-source/`는 실제 원본도 배포 대상도 아닌 격리 참고 스냅샷으로 분류했다.
- 앞으로 코딩 수정은 이 repo의 `src/`에 반영하고, 빌드 결과를 게시 파일에 반영한다.
- 수량 원칙을 정리했다. 구역 수량 계산은 작업 순서 기준이고, 미주 구역명에 붙는 특수 로직은 1/2/3동/나머지 상세 분해뿐이다.
- 도우미 배송 무료/유료를 원본 코드에 반영했다. 무료는 총수량 포함/효율 제외, 유료는 총수량 포함/효율 포함으로 계산한다.
- 백업설정의 기록 정정에서 완료 구역을 도우미 배송 무료/유료로 전환할 수 있게 했다. 로그 화면은 보기 전용으로 유지한다.
- 도우미 배송 수량도 2구역 이후 전체수량 차감 대상에 포함한다.

# 배송마스터 개발앱 진행 기록

현재 작업 범위와 수정 파일을 기록한다.

## 2026-06-07 직접배송 공통 검수 확장

사용자 지시:

- 현장 장애는 일반 절차보다 핫픽스 우선으로 처리해야 한다.
- 이번 문제는 힐스만이 아니라 대체배송/추가구역에도 같은 유형으로 발생할 수 있으므로 전 구역 공통으로 정리한다.

코기 이해:

- v13 코드 수정은 미주 외 공통 구역 완료 조건을 바꿨기 때문에 대체배송/추가구역에도 적용된다.
- 다만 검수 항목이 힐스에만 있어 부족했다.
- 긴급 대응 기준은 최상위 작업 규칙에 남긴다.

승인 상태:

- 승인됨. 관련 문서와 검수 케이스를 정리한다.

수정 파일:

- `instruction.md`
- `scripts/browser-smoke.mjs`
- `todo.md`
- `progress.md`
- `changelog.md`

검수 계획:

- `node --check scripts/browser-smoke.mjs`
- `npm run check`
- `npm run build`
- `npm run check:codex`
## 2026-06-07 바로 배송 시작 완료 저장 핫픽스

사용자 지시:

- 힐스테이트를 첫 구역으로 변경하고 `바로 배송 시작`한 뒤 수량 13개 완료가 저장되지 않는다.
- 오늘은 대체배송까지 풀로 써야 하므로 즉시 배포가 필요하다.

코기 이해:

- 수량 13은 정상 값이다.
- 버그는 미주 외 구역 완료 저장 로직이 `바로 배송 시작` 루트에도 `정리 완료`를 요구하는 데 있다.
- `정리 시작`을 누른 구역만 `정리 완료`를 요구하고, `바로 배송 시작` 루트는 수량 완료를 허용해야 한다.

승인 상태:

- 승인됨. 즉시 배포까지 진행한다.

예상 수정 파일:

- `src/app/main.ts`
- `src/app/version.ts`
- `scripts/browser-smoke.mjs`
- `todo.md`
- `progress.md`
- `changelog.md`

검수 계획:

- `npm run check`
- `npm run build`
- 힐스 첫 구역 `바로 배송 시작 -> 13개 완료` 검수 케이스 추가
## 2026-06-06 정리 UI / 통계 비율 최종 보정

사용자 지시:

- 정리 시작 후에는 `정리 완료`가 최상위 행동으로 보여야 한다.
- `보정 적용`, `정리 없음`이 최상위에 있으면 현장에서 불편하다.
- 주간/월간 통계 제목 옆 수량비는 의미가 약하므로 `미50:힐40:대10` 같은 백분율 요약으로 바꾼다.
- 이번 수정은 배포까지 진행한다.

코기 이해:

- 저장 데이터와 계산 로직은 건드리지 않고 UI 행동 순서와 통계 표시만 보정한다.
- 힐스테이트뿐 아니라 정리 흐름을 공유하는 대체배송/추가구역에도 같은 기준을 적용한다.
- 통계 상세 수량과 막대는 유지하고, 카드 상단 배지만 백분율 요약으로 바꾼다.

승인 상태:

- 승인됨. 배포까지 진행한다.

예상 수정 파일:

- `src/app/main.ts`
- `scripts/browser-smoke.mjs`
- `src/app/version.ts`
- `todo.md`
- `progress.md`
- `changelog.md`

검수 계획:

- `npm run check`
- `npm run build`
- 411x762 / DPR 2.63 브라우저 스모크 검수
- 정리 보정 패널에서 `정리 완료`가 보정/정리없음보다 위에 있는지 확인
- 주간/월간 통계 배지가 백분율 요약으로 표시되는지 확인
## 2026-06-03 승인 기록 / 작업 트리 훅 보강

사용자 지시:

- 작업은 코기가 이해한 내용을 짧게 보고하고, 오빠가 승인한 뒤에만 시작한다.
- 승인 직후에는 작업 시작 기록을 남긴다.
- 작업 후 문서 업데이트와 후속 조치가 빠지지 않도록 구조를 보강한다.

코기 이해:

- 승인은 작업의 전제다. 승인 없이는 작업하지 않는다.
- 승인 기록은 오빠에게 추가 부담을 주는 절차가 아니라 맥락 압축과 후속 검수용 흔적이다.
- 훅은 승인 자체를 판정하지 않고, 코드 변경 시 `progress.md`와 `changelog.md` 누락을 막는 역할을 한다.

승인 상태:

- 승인됨.

예상 수정 파일:

- `instruction.md`
- `progress.md`
- `changelog.md`
- `todo.md`
- `scripts/codex-precommit-check.mjs`

검수 계획:

- `node --check scripts/codex-precommit-check.mjs`
- `npm run check:codex`
- staged dry-run으로 `progress.md` / `changelog.md` 동반 규칙 확인
## 2026-06-03 공용 규칙 적용 준비

목표:

- `C:\Codex55Workspace\_ProjectCommonRules`의 공용 작업 트리와 훅 기준을 배송마스터 현재 구조에 맞게 재판단한다.
- 기존 PWA 현장앱 v1/season2, 현재 개발앱 PWA, GitHub Pages 배포 repo의 역할을 문서에 명확히 남긴다.
- Hermes/OpenClaw 서버 운영 전제는 배송마스터 개발앱에는 기본 적용하지 않는다.

수정 파일:

- `instruction.md`
- `README.md`
- `todo.md`
- `progress.md`
- `changelog.md`

판단:

- 현장앱 v1과 season2는 보관/백업용 과거 세대 원본이며, 명시 요청 없이는 수정하지 않는다.
- 현재 실사용 및 개발 source-of-truth는 `delivery-master-install` 개발앱 PWA다.
- `delivery-master-install-deploy`는 배포 산출물 repo이며 source-of-truth가 아니다.
- 공용 작업 트리 중 source-of-truth 확인, 문서 우선, Git 상태 확인, 위험 작업 승인, 사용자 실제 화면 기준 완료 원칙은 적용한다.
- 서버 동기화, systemd, MCP, Zulip/Hermes 보고서 완료 조건은 기본 적용하지 않는다.
- 개발앱 원본용 프로젝트 맞춤 pre-commit hook 초안과 dry-run 스크립트를 작성했다.
- hook은 경로, source-of-truth 폴더명, changelog 동반 여부, 금지 폴더, 민감정보 패턴, `git diff --check`를 빠르게 검사한다.
- 무거운 domain/browser 테스트는 hook에 넣지 않고 별도 검수로 유지한다.

## 2026-06-02 같은 분 로그 순서 보정

목표:

- 로그 화면에서 같은 분에 `이전 구역 완료`와 `다음 구역 시작`이 겹칠 때 현장 업무 흐름이 뒤집히지 않게 한다.
- 이번 현장 증상을 브라우저 검수 항목에 추가해 같은 실수가 배포 직전에 다시 통과하지 않게 한다.

수정 파일:

- `src/app/main.ts`
- `scripts/browser-smoke.mjs`
- `instruction.md`
- `todo.md`
- `unresolved.md`
- `progress.md`
- `changelog.md`

진행 메모:

- 원본 `DayRecord.timeline` 시간은 바꾸지 않는다.
- 로그 화면 표시 정렬에서만 같은 분의 `zone_end -> zone_start` 흐름을 보정한다.
- 검수 항목 `previousZoneEndBeforeNextZoneStart`를 추가한다.

## 현재 작업

날짜: 2026-05-27

목표:

- 코드 수정 전에 운영 문서 체계를 먼저 정리한다.
- 오빠가 `instruction.md`만 확인해도 전체 작업 규칙을 파악할 수 있게 한다.
- 운영 문서 내용은 한국어 중심으로 바꾸고, 파일명과 코드 식별자는 영어 체계를 유지한다.
- 문서 깨짐을 임의 복구하지 않도록 UTF-8/인코딩 규칙을 추가한다.

승인:

- 승인됨. 사용자가 문서 정리와 한국어화 방향을 승인했다.

## 이번 작업 수정 파일

- `instruction.md`
- `todo.md`
- `progress.md`
- `unresolved.md`
- `changelog.md`

## 진행 메모

- 앱 코드는 수정하지 않는다.
- 기존 깨진 문서는 바로 고치지 않고 `unresolved.md`에 미해결 문제로 등록한다.
- 다음 코드 작업은 반드시 `instruction.md`를 읽고, 이 문서에 새 작업 범위를 작성한 뒤 시작한다.
- `instruction.md` 7번은 현재 버그 목록이 아니라 실제 앱/브라우저 검수 규칙만 담도록 정리한다.
- 미주/힐스 같은 현재 문제 목록은 `unresolved.md`와 `todo.md`에서 관리한다.
- 새 지시나 수정사항을 받으면 코기가 먼저 파악한 내용을 보고하고, 오빠가 판단한 뒤 작업한다는 규칙을 `instruction.md`에 추가한다.
- 개발앱의 구역 진행 순서 변경 기능 부족을 미해결 문제와 TODO에 등록한다.
- 구역 순서 변경 요구사항은 미주, 힐스테이트, 대체배송지, 추가구역을 화살표로 위아래 이동하는 이전 현장앱 방식을 기준으로 한다.
- 특정 구역에서 발견된 문제라도 전 구역에 반복될 수 있으면 공통 구조 문제로 승격한다는 규칙을 `instruction.md`에 추가한다.
- 미주/힐스 개별 증상으로 적어둔 입력 문제를 전 구역 공통 입력 검증, 자동계산, 이상 수량 방어 문제로 재정리한다.

## 다음 코드 작업 계획 초안

목표:

- 개발앱의 완료 저장 전 공통 입력 안정성 레이어를 만든다.
- 전 구역에서 자료 누락, 이상 수량, 총합/부분값 자동계산을 같은 기준으로 처리한다.
- 구역 순서 변경 기능과 모바일 입력 배치 문제는 같은 작업 묶음에서 영향도를 확인하되, 공통 입력 안정성을 우선한다.

예상 작업 범위:

- 구역 완료 저장 직전 공통 검증 함수 확인 및 보강
- 완료 기록 수정 저장 직전 공통 검증 함수 확인 및 보강
- 총합 입력값과 이미 완료된 구역 합계를 이용한 자동계산 흐름 설계
- 미주 상세값처럼 구역별 특수값은 공통 검증 위에 얹는 방식으로 정리
- 구역 순서 변경 UI가 현재 구조에 어디서 들어가야 하는지 확인
- 실패/추가 입력칸 모바일 배치 정리 위치 확인

검수 계획:

- 실제 앱 또는 브라우저에서 정상 루트 1개 이상 입력한다.
- 미주, 힐스테이트, 대체배송지 중 최소 2개 이상에서 실수 루트를 입력한다.
- 자료 누락 완료, 전체 수량 오입력, 자동계산, 완료 기록 수정, 새로고침 후 유지 여부를 확인한다.

## 2026-05-28 작업 결과

수정 파일:

- `src/app/main.ts`
- `src/domain/zoneValidation.ts`
- `public/styles.css`
- `dist/*`
- `scripts/run-domain-tests.ts`
- `scripts/browser-smoke.mjs`
- `review-mobile.png`
- `todo.md`
- `unresolved.md`
- `progress.md`
- `changelog.md`

반영:

- 전 구역 공통 수량 검증 레이어를 추가했다.
- 빈 수량이나 0개 수량으로 구역 완료가 저장되지 않게 했다.
- 당일 전체 수량처럼 보이는 값을 입력하면 이미 완료된 구역 수량을 빼서 현재 구역 수량으로 저장할 수 있게 했다.
- 미주 총합과 1/2/3동 상세를 같이 입력하면 나머지 수량을 자동 계산한다.
- 완료 기록 수정에서 실패/추가 입력칸을 별도 줄로 분리했다.
- 작업 시작 전 구역 순서를 화살표로 위아래 조정하고 대체배송/추가구역을 붙일 수 있게 했다.

검수:

- `npm.cmd run check`: 통과, 도메인 테스트 46/46.
- `npm.cmd run build`: 통과.
- 실제 브라우저 모바일 크기 검수 통과.
- 브라우저 검수 항목: 순서 화살표 이동, 대체배송 추가, 미주 총합 자동계산, 힐스 전체수량 오입력 보정, 빈 수량 완료 차단.

남은 판단:

- 이번 변경은 GitHub Pages 배포까지 완료했다.
- 배포 커밋: `8c38bd5 Deploy field guardrails update`
- 배포 확인: `https://owenyu9292.github.io/delivery-master-install/sw.js`에서 `delivery-master-install-v8` 확인.
- 다음 단계는 2026-05-28 현장 테스트 후 실제 사용 문제를 다시 수집하는 것이다.

## 2026-05-28 운영 기준점 전환

목표:

- 개발앱을 그때그때 수정하는 방식에서 git 기준점과 작업 규칙으로 굴러가는 방식으로 전환한다.
- 오늘 현장에서 에러 없이 사용한 `v8 / 0.2.7-field-guardrails`를 첫 이동 기준점으로 삼는다.
- 고정된 과거 원본이 아니라 현장에서 사용 가능한 최신 안정 상태를 원본으로 본다.

반영 원칙:

- 코기는 사용자가 직접 챙기기 어려운 개발 기본 안전장치가 빠져 있으면 먼저 제안한다.
- 반복되는 문제가 보이면 코드 한 줄 수정이 아니라 작업 방식 자체를 점검한다.
- 이후 개발앱 수정은 git 기준점과 브랜치를 사용한다.

진행 상태:

- `instruction.md`에 git 기준점과 개발 안전장치 제안 규칙을 추가했다.
- 개발앱용 `.gitignore`를 추가했다.
- 개발앱 폴더에 git 저장소를 초기화했다.
- 첫 기준점 커밋을 만들었다: `f98410d Baseline field-tested v8 guardrails`
- 다음 작업 브랜치를 만들었다: `codex/field-ux-redesign`

## 다음 UX 재설계 방향

목표:

- 개발앱을 오빠 개인 현장앱답게 빠르고 간편하게 만든다.
- 출발/도착 이후 바로 작업 순서와 구역 입력이 자연스럽게 이어지게 한다.
- 입력 흐름을 현장 실제 순서에 맞춘다.
- 이후 넣기로 한 조회/통계/리포트 화면도 현재 구조에 맞춰 기본 골격을 만든다.

우선 반영할 흐름:

- 작업 순서: `미주 / 힐스테이트 / 대체배송 / 추가구역`을 기본 목록으로 보여주고, 화살표로 순서만 바꾼다.
- 미주: `1동 / 2동 / 3동` 입력을 먼저 보여주고, 버튼명은 `1/2/3동 기록`으로 한다.
- 미주: 이후 전체 수량 입력으로 나머지 수량을 자동 계산한다.
- 힐스/대체/추가: 시작 후 `정리 시작`과 `바로 배송 시작` 중 하나만 고르게 한다.
- 힐스/대체/추가: `정리 시작`을 누르면 다음 버튼은 `정리 완료`, 그 다음 배송 진행으로 이어진다.
- 전체 수량 입력은 정상 사용 방식으로 보고, 불필요한 확인창 없이 자동 계산한다.
- 리포트 시간 표시는 분 단독이 아니라 `n시간 n분` 중심으로 바꾼다.
- 업무 종료 후 저장/백업 상태를 명확히 알려준다.

기본 앱 모양:

- 입력 화면: 현장 순서형.
- 조회 화면: 날짜별 기록 확인.
- 리포트 화면: 당일 리포트 보기와 복사.
- 통계 화면: 주간/월간 물량, 구역별 비율, 시간/효율 요약.
- 백업 화면: 자동저장/백업/가져오기 상태 확인.

진행 원칙:

- 한 번에 모든 통계를 완성하려 하지 않고, 기본 화면 골격과 핵심 숫자부터 만든다.
- 현장 입력을 방해하는 버튼, 확인창, 접힌 입력창은 줄인다.
- 코딩 전에는 이 계획을 기준으로 예상 수정 파일과 브라우저 검수 항목을 다시 보고한다.

## 2026-05-28 현장 입력 UX 1차 작업

승인:

- 승인됨. 작업은 `codex/field-ux-redesign` 브랜치에서 진행한다.
- 배포는 수정과 검수 후 별도 승인받는다.

범위:

- 작업 순서 화면을 기본 구역 목록과 화살표 조정 방식으로 바꾼다.
- 미주는 1동/2동/3동 입력을 먼저 보여주고, `1/2/3동 기록` 후 전체 수량으로 나머지를 자동 계산한다.
- 완료 기록 수정에서도 미주 나머지 수량은 직접 계산하게 하지 않는다.
- 힐스테이트/대체배송/추가구역은 `정리 시작` 또는 `바로 배송 시작` 중심의 간편 흐름으로 정리한다.
- 리포트와 화면의 시간 표시는 `n시간 n분` 중심으로 정리한다.
- 업무 종료 후 저장/백업 상태를 알 수 있게 피드백을 남긴다.

브라우저 검수 항목:

- 정상 입력 루트.
- 작업 순서 화살표 이동.
- 미주 1/2/3동만 입력하고 완료 시도.
- 전체 수량 없이 미주 완료 시도.
- 전체 수량이 1/2/3동 합보다 작은 경우.
- 힐스테이트 무입력 완료 시도.
- 말도 안 되게 큰 수량 입력 시도.
- 정리 시작/정리 완료/바로 배송 시작 흐름.
- 업무 종료 후 저장 피드백.
- 새로고침 후 기록 유지.

작업 결과:

- `src/app/main.ts`에서 현장 순서형 입력 흐름을 1차 반영했다.
- `src/domain/reportBuilder.ts`에서 리포트 시간 표기를 `n시간 n분` 중심으로 맞췄다.
- `scripts/browser-smoke.mjs`에 정상 루트, 실수 루트, 큰 수량 입력 루트를 추가했다.
- `review-mobile.png`를 새 화면 기준 브라우저 검수 결과로 갱신했다.

검수 결과:

- `npm.cmd run check`: 통과, 도메인 테스트 46/46.
- `npm.cmd run build`: 통과.
- 실제 브라우저 모바일 크기 검수: 통과.
- 확인한 항목: 작업 순서 화살표 이동, 미주 1/2/3동 선기록, 미주 전체 수량 누락 차단, 미주 나머지 자동계산, 힐스 정리 흐름, 힐스 무입력 완료 차단, 말도 안 되게 큰 수량 저장 방지.

남은 판단:

- 이번 변경은 오빠 승인 후 GitHub Pages 배포 저장소에 반영했다.
- 앱 버전은 `0.2.8-field-ux`, 서비스워커 캐시는 `delivery-master-install-v9`로 올렸다.
- 배포 커밋은 `ccfe9e8 Deploy field UX update`이다.
- GitHub 원격 저장소 main의 `sw.js`는 `v9` 확인 완료.
- GitHub Pages URL은 배포 직후 잠시 `v8`을 줄 수 있으므로 반영 지연 여부를 추가 확인한다.

## 2026-05-29 필드 피드백 기반 다음 작업 준비

목표:

- v9 입력 UX는 대체로 실사용에 맞았으므로 유지한다.
- 확인창, 로그, 리포트, 통계, 백업설정 화면을 현장앱 기준으로 보강한다.
- 개발앱은 입력 안정성은 유지하고, 확인/조회 경험은 현장앱의 좋은 흐름을 계승한다.

필드 피드백:

- 힐스테이트에 당일 전체 수량 `292`를 입력하면 `150`을 빼 `142`로 계산하는 것은 맞지만, 확인창이 뜨면 안 된다.
- 사용자는 평소 리포트보다 로그 화면을 더 많이 본다.
- 현장앱 로그는 출발/도착/구역 시작/미주 상세/정리/완료가 시간순으로 잘 보여서 개발앱도 이 기준을 따라야 한다.
- 개발앱 리포트는 운전시간과 출발/도착/종료 시간축이 부족하다.
- 주간/월간 막대와 퍼센트 표시는 좋지만 상단의 원시 비율 배지는 다듬어야 한다.
- 설정 탭은 일반 설정이 아니라 `백업설정`으로 보고, 백업/가져오기/초기화/버전 확인을 모은다.

예상 수정 파일:

- `src/app/main.ts`
- `src/domain/reportBuilder.ts`
- `scripts/browser-smoke.mjs`
- `todo.md`
- `progress.md`
- `unresolved.md`
- `changelog.md`
- 필요 시 `public/styles.css`

검수 계획:

- 뒤 구역 전체 수량 자동차감 중 확인창이 뜨지 않는지 확인한다.
- 로그 탭만 보고 2026-05-29 하루 흐름을 복구할 수 있는지 확인한다.
- 리포트에 진접 출발, 청량리 도착, 순수 운전, 최종 종료가 표시되는지 확인한다.
- 리포트 구역별 상세에 미주/힐스 시작~종료와 힐스 정리 시간이 표시되는지 확인한다.
- 주간/월간 비율 배지가 읽기 쉬운 표현인지 확인한다.
- 백업설정 탭에 백업/가져오기/초기화/버전 정보가 분리되어 있는지 확인한다.

작업 결과:

- `업무 / 로그 / 리포트 / 통계 / 백업설정` 탭 구조를 구현했다.
- 뒤 구역 전체 수량 자동차감은 확인창 없이 토스트 안내 후 저장하도록 바꿨다.
- 로그 탭은 현장앱 기준으로 출발, 도착, 구역 시작, 정리 시작/완료, 구역 완료, 업무 종료를 시간순으로 보여준다.
- 리포트는 현장앱 포맷 기준으로 재구성하고 순수 운전, 출발/도착/최종 종료, 구역별 상세를 포함했다.
- 주간/월간 비율 배지는 원시 비율 대신 `미주 n% / 힐스 n% / 대체배송지 n%` 형식으로 바꿨다.
- 백업설정 탭에 백업 내보내기, 현장앱 백업 가져오기, 오늘 초기화, 버전 확인을 모았다.

검수 결과:

- `npm.cmd run check`: 통과, 도메인 테스트 46/46.
- `npm.cmd run build`: 통과.
- 실제 브라우저 검수: 기존 입력 루트 통과.
- 실제 브라우저 탭 검수: 로그, 리포트, 통계, 백업설정 표시 통과.
- 리포트 검수: `순수 운전`, `진접`, `최종 종료` 표시 확인.

남은 판단:

- 구현과 검수는 완료됐다.
- 아직 GitHub Pages 배포는 하지 않았다.
- 배포 전 앱 버전과 서비스워커 캐시 버전을 올려야 한다.

추가 반영:

- 앱 상단과 백업설정에 `v0.2.9-field-log-report · 2026-05-29 수정본` 표시를 추가했다.
- 목적은 현장에서 최신 수정본 반영 여부를 바로 확인하기 위함이다.
- `npm.cmd run check`: 통과, 도메인 테스트 46/46.
- `npm.cmd run build`: 통과.
### 2026-05-29 앱 버전/캐시 버전 정리

추가 반영:

- 앱 상단 긴 버전 표시를 `v10` 중심의 짧은 표시로 정리했다.
- 백업설정에는 상세 버전과 수정일, cache 버전을 유지했다.
- `src/app/version.ts`를 추가해 앱 표시 버전과 서비스워커 캐시 버전의 단일 기준을 만들었다.
- 빌드 시 `dist/sw.js`가 단일 기준의 `CACHE_NAME`을 받아가도록 `scripts/build-app.mjs`를 수정했다.

검증 결과:

- `node --check scripts/build-app.mjs`: 통과.
- `npm.cmd run check`: 통과, 도메인 테스트 46/46.
- `npm.cmd run build`: 통과.
- 빌드 결과 `dist/sw.js`의 캐시명은 `delivery-master-install-v10`으로 확인했다.

## 2026-05-30 필드 안정성 로컬 수정

목표:

- 현장 사용 중인 Pages 배포본은 건드리지 않고 개발앱 소스만 수정한다.
- 출발, 도착 후 첫 구역 시작, 완료 기록 수정, 업무 종료 백업에서 현장 실수와 데이터 손실 가능성을 낮춘다.
- 전날 검수에서 놓친 항목을 브라우저 스모크 검수에 추가한다.

수정 파일:

- `src/app/main.ts`
- `src/domain/zoneValidation.ts`
- `src/domain/fieldAppMigration.ts`
- `src/storage/indexedDbAdapter.ts`
- `src/ui/uiScreens.ts`
- `scripts/browser-smoke.mjs`
- `review-mobile.png`

반영 내용:

- 출발 예상 수량 빈칸은 더 이상 0으로 처리하지 않는다. 무보수 도우미날은 사용자가 `0`을 직접 입력하고 확인해야 한다.
- 청량리 도착 후 기본 구역 순서를 자동으로 만들고, 현재 구역 시작 버튼을 작업 순서 편집 UI보다 위에 배치했다.
- 오늘 초기화와 완료 기록 수정 전에 실제 백업 JSON 다운로드를 시작하게 했다.
- 업무 종료 시 전체 개발앱 백업 파일 다운로드를 자동으로 시작하게 했다.
- 완료 기록 수정에서 시작/종료/정리 시작/정리 완료의 시간 역전을 차단한다.
- 정리 시작/정리 완료 중복 클릭은 같은 이벤트가 이미 있으면 무시한다.
- 뒤 구역 전체 수량 자동차감은 입력값이 당일 총량 근처일 때만 적용하도록 좁혔다.
- 현장앱 백업 가져오기에서 미주 A/B 상세값이 리포트까지 이어지도록 `zone_end` payload에도 복사했다.
- 통계 분류에서 `zone-1-miju` 같은 imported id도 미주로 인식하게 했다.
- 백업 `appVersion`은 `src/app/version.ts` 기준값을 저장소에 넘겨 사용하게 했다.
- 서비스워커 등록 실패가 앱 부팅 전체를 막지 않게 했다.

검수 결과:

- `npm run check`: 통과, 도메인 테스트 46/46.
- `npm run build`: 통과.
- headless 브라우저 검수: 통과.
- 추가 검수 항목: 빈 출발 수량 차단, 현재 구역 시작 버튼이 순서 편집보다 위에 있는지, 미주 전체 수량 누락 차단, 미주 나머지 자동계산, 힐스 전체 수량 자동차감, 무입력/대형 수량 실수 루트.

상태:

- 로컬 소스 수정과 검수만 완료했다.
- GitHub Pages 배포와 push는 하지 않았다.

## 2026-05-30 공통 구역 UI 현장 순서 보정

목표:

- 힐스테이트, 대체배송, 추가구역 공통 UI를 코드 상태 순서가 아니라 현장에서 다음에 누르는 순서로 재배치한다.
- 같은 공통 컴포넌트를 쓰는 구역은 한 구역만 보지 않고 전 구역 공통 규칙으로 본다.
- 검수 기준에 "버튼이 작동하는가"뿐 아니라 "다음 행동이 최상단 주버튼인가"를 추가한다.

수정 파일:

- `instruction.md`
- `src/app/main.ts`
- `public/styles.css`
- `scripts/browser-smoke.mjs`
- `review-mobile.png`
- `progress.md`
- `todo.md`
- `unresolved.md`
- `changelog.md`

반영 내용:

- `instruction.md`에 버튼 배치는 코드 상태 순서가 아니라 현장에서 다음에 누를 순서가 우선이라는 규칙을 추가했다.
- 힐스테이트, 대체배송, 추가구역이 함께 쓰는 공통 UI에서 `정리 시작`을 최상단 주버튼으로 두고 `바로 배송 시작`은 보조 버튼으로 내렸다.
- 정리 중 화면에서는 `정리 완료` 버튼을 안내문보다 먼저 보여준다.
- 수량 입력 단계에서는 `전체 수량` 입력칸과 완료 버튼을 먼저 보여주고, 보조 설명은 아래로 내렸다.
- 보조 버튼 스타일을 추가해 예외 행동이 주버튼처럼 보이지 않게 했다.
- 브라우저 스모크 검수에 공통 구역 UI 순서 검증을 추가했다.

검수 결과:

- `npm run check`: 통과, 도메인 테스트 46/46.
- `npm run build`: 통과.
- headless 브라우저 검수: 통과.
- 새 검수 항목: `genericInitialUiOrder`, `genericSortingUiOrder`, `genericCountUiOrder` 모두 통과.

상태:

- 로컬 소스 수정과 검수만 완료했다.
- GitHub Pages 배포와 push는 하지 않았다.

추가 문서 정리:

- `instruction.md` 최상단에 `0. 최상위 설계 원칙: 사용자 편의`를 추가했다.
- 사용자 편의, 현장 다음 행동 우선, 주버튼/보조버튼 우선순위, 공통 UI 전체 적용, 검수 기준을 절대 규칙보다 위에 두었다.
- 문서 인코딩 규칙을 보강했다. 한국어 문서는 처음부터 UTF-8 기준 도구로 읽고, 깨져 보이는 PowerShell 출력은 작업 근거로 쓰지 않는다.
- 인코딩 규칙을 `0-1. 최상위 작업 전제: 한국어 문서 UTF-8 우선`으로 승격했다.
- 작업 전 절차에도 한국어 문서는 UTF-8 기준 도구로 확인한다는 항목을 추가했다.

## 2026-05-30 최상위 규칙 승격 체계 보강

작업 범위:
- 중요 지시가 `progress.md`, `todo.md`, `changelog.md` 같은 하위 문서에만 남고 `instruction.md`로 올라가지 않는 문제를 작업 시스템 문제로 정리했다.
- 새 지시가 기능 요청, 버그 수정, 최상위 규칙 후보 중 어디에 속하는지 작업 전에 판단하도록 했다.

반영:
- `instruction.md`에 `0-2. 최상위 작업 전제: 중요 지시 승격과 문서 위치`를 추가했다.
- 데이터 보존과 `DayRecord.timeline` 원본 우선 원칙을 `0-3. 최상위 데이터 원칙`으로 승격했다.
- 실제 앱/브라우저 검수 없이는 현장 흐름 작업을 완료 처리하지 않는 원칙을 `0-4. 최상위 검수 원칙`으로 승격했다.
- 작업 전 절차에 최상위 규칙 후보 판단과 `instruction.md` 반영 위치 보고를 추가했다.

상태:
- 문서 수정만 진행했다.
- 코드 수정, 빌드, 배포, push는 하지 않았다.

## 2026-05-30 완료 기록 수정 폼 및 시간축 검증 보강

작업 범위:
- 현장 업무 마감 후 배포 전 로컬 수정으로 진행했다.
- 완료 기록 수정 화면이 모바일 폭에서 잘리는 문제와 날짜까지 노출되는 문제를 수정했다.
- 로그에서 청량리 도착과 구역 시작이 같은 분 안에서 뒤집혀 보일 수 있는 문제를 수정했다.
- 시간축 원본 자체가 틀어지지 않도록 완료 기록 수정 저장 전 전체 업무 순서 검증을 추가했다.

반영:
- 완료 기록 수정 폼의 시작/종료/정리 시작/정리 완료 입력을 `datetime-local`에서 시간 전용 입력으로 변경했다.
- 시간 저장 시 현재 업무 날짜와 입력 시간을 합쳐 ISO 값으로 저장하고, 기존 이벤트 초 단위는 가능한 보존한다.
- 구역 시작이 청량리 도착보다 빠른 경우, 이전 구역 완료보다 빠른 경우, 구역 종료가 다음 구역 시작보다 늦은 경우, 구역 종료가 업무 종료보다 늦은 경우를 차단한다.
- 로그 정렬은 같은 분 안에서 `진접 출발 -> 청량리 도착 -> 구역 시작 -> 정리/배송 -> 구역 완료 -> 업무 종료` 순서를 우선한다.
- 모바일 검수 기본 폭을 430x932로 보고, 데스크톱 전체 화면 검수는 현장 UI 검수로 인정하지 않는 규칙을 `instruction.md`에 추가했다.

검수 결과:
- `npm run check`: 통과, 도메인 테스트 46/46.
- `npm run build`: 통과.
- 모바일 폭 브라우저 검수: 통과.
- 추가 검수 항목: `mobileViewport`, `editFormMobileOk`, `timelineEditBlocked`, `arriveBeforeMijuStart` 모두 통과.

상태:
- 로컬 소스 수정과 검수만 완료했다.
- GitHub Pages 배포와 push는 하지 않았다.

## 2026-05-30 갤럭시 폴드 앞 화면 기준 검수 보강

작업 범위:
- 기존 모바일 검수 기준 `430x932`는 일반 모바일 폭이라 오빠가 실제로 쓰는 접힌 폴드 앞 화면 검수 기준으로 부족했다.
- 최상위 검수 원칙을 갤럭시 폴드 앞 화면 기준으로 보강했다.
- 앱 안에서 실제 CSS 화면값을 확인할 수 있도록 `백업설정`에 화면 정보를 추가했다.
- 브라우저 스모크 검수 기본값을 `360x800 / DPR 3`으로 낮춰 더 좁은 화면에서 다시 검수했다.

수정 파일:
- `instruction.md`
- `src/app/main.ts`
- `scripts/browser-smoke.mjs`
- `review-fold-cover-360x800.png`
- `todo.md`
- `progress.md`
- `changelog.md`

검수 결과:
- `node --check scripts/browser-smoke.mjs`: 통과.
- `npm run check`: 통과, 도메인 테스트 46/46.
- `npm run build`: 통과.
- 브라우저 스모크 검수: `360x800 / DPR 3`에서 통과.
- 통과 항목: `mobileViewport`, `editFormMobileOk`, `timelineEditBlocked`, `arriveBeforeMijuStart`, `backupShowsViewport` 포함 전체 스모크 항목 통과.

상태:
- GitHub Pages 배포와 push까지 완료했다.
- 배포 저장소 커밋: `ede9022 Deploy fold cover field fixes`
- 원격 확인: `https://owenyu9292.github.io/delivery-master-install/sw.js`에서 `delivery-master-install-v11` 확인.
- 실제 폴드 앞 화면의 CSS 값은 앱에서 `백업설정 > 화면 정보`를 확인해 다음 검수 기준에 반영한다.

## 2026-05-30 실제 폴드 앞 화면 검수 기준 반영

작업 범위:
- 실제 기기 `백업설정 > 화면 정보`에서 확인한 CSS 화면값 `411x762 / DPR 2.63`을 기본 검수 기준으로 반영했다.
- 기존 `360x800 / DPR 3`은 기본값이 아니라 좁은 폭 보수 검수 옵션으로 낮췄다.

수정 파일:
- `instruction.md`
- `scripts/browser-smoke.mjs`
- `todo.md`
- `progress.md`
- `changelog.md`
- `unresolved.md`

검수 계획:
검수 결과:
- `node --check scripts/browser-smoke.mjs`: 통과.
- 브라우저 스모크 검수: `411x762 / DPR 2.63`에서 통과.
- 통과 항목: `mobileViewport`, `editFormMobileOk`, `timelineEditBlocked`, `arriveBeforeMijuStart`, `backupShowsViewport` 포함 전체 스모크 항목 통과.

상태:
- 로컬 소스 수정과 검수 완료.
- GitHub Pages 배포는 하지 않았다.

## 2026-05-30 현장앱 통계 구조 비교

작업 범위:
- 현장앱 `season2/index.html`, `season2/js/analysis.js`의 주간/월간/조회 구조를 확인했다.
- 개발앱 `src/app/main.ts`, `src/ui/uiScreens.ts`의 현재 통계 구현과 비교했다.

파악:
- 현장앱은 리포트 화면 안에 `오늘 / 주간 / 월간 / 조회` 서브탭이 있다.
- 주간은 `currentWeekOffset`으로 이전주/다음주를 계속 넘기며 주 범위, 총 배송, 근무일, 평균 효율, 스캔미스, 구역별 효율, 일별 현황을 보여준다.
- 월간은 `currentMonthOffset`으로 이전달/다음달을 계속 넘기며 월간 총계, 최다/최소 배송일, 일 평균 배송, 구역별 월간 효율, 요일별 평균을 보여준다.
- 조회는 날짜 선택 후 해당 날짜 리포트와 로그를 같이 보여준다.
- 개발앱은 현재 `이번 주 / 이번 달` 수량 비교만 있어 기간 탐색과 날짜 조회가 빠져 있다.

판정:
- 통계 기능은 현장앱 대비 퇴보다.
- 개발앱 통계 탭을 `주간 / 월간 / 날짜조회` 서브탭 구조로 보강해야 한다.
- 미주/힐스테이트/대체배송 비율카드는 개발앱 통계의 핵심 지표다. 주간/월간 화면 최상단에서 가장 먼저 읽히게 둔다.
- 통계 화면은 계산/코딩 순서가 아니라 오빠가 현장에서 월간 실적 편차를 빨리 보는 순서로 설계한다.

## 2026-05-30 개발앱 통계 탭 기간 탐색 보강

작업 범위:
- 개발앱 `통계` 탭을 현장앱 사용감에 맞춰 `주간 / 월간 / 날짜조회` 서브탭으로 보강했다.
- 비율카드를 주간/월간 화면의 첫 핵심 카드로 유지했다.
- 주간/월간 기간 이동과 날짜별 로그/리포트 조회를 추가했다.

반영:
- 주간 통계: 이전주/다음주 이동, 주 범위, 비율카드, 총 배송, 근무일, 평균 효율, 스캔차, 배송 시간, 일 평균, 구역별 요약, 일별 현황을 표시한다.
- 월간 통계: 이전달/다음달 이동, 월 제목, 비율카드, 총계/효율 요약, 최다/최소 배송일, 구역별 요약, 요일별 평균, 해당 월 기록 목록을 표시한다.
- 날짜조회: 날짜 선택 후 해당 날짜의 요약, 로그, 리포트를 한 화면에서 확인한다.
- 브라우저 스모크 검수에 `statsRatioCardFirst`, `statsRatioLabel`, `statsWeekNavigation`, `statsMonthNavigation`, `statsDateSearch`를 추가했다.

검수 결과:
- `npm run check`: 통과, 도메인 테스트 46/46.
- `npm run build`: 통과.
- 브라우저 스모크 검수: `411x762 / DPR 2.63`에서 통과.
- 통계 검수 항목: `statsRatioCardFirst`, `statsRatioLabel`, `statsWeekNavigation`, `statsMonthNavigation`, `statsDateSearch` 모두 통과.

상태:
- 로컬 소스 구현과 검수 완료.
- GitHub Pages 배포는 아직 하지 않았다.

## 2026-05-30 정기휴무 가상표시 보강

작업 범위:
- 일요일/월요일 고정 휴무를 저장 원본으로 만들지 않고, 과거 빈 날짜 화면 표시로만 처리했다.
- 통계 주간/월간 일별 목록과 날짜조회에 정기휴무 가상표시를 추가했다.
- 백업 JSON에는 휴무 빈 기록을 새로 만들지 않는 원칙을 최상위 데이터 원칙에 추가했다.

반영:
- 과거 일요일/월요일에 `DayRecord`가 없으면 `정기휴무`로 표시한다.
- 오늘과 미래 날짜는 자동 휴무로 표시하지 않는다.
- 날짜조회에서 과거 정기휴무 빈 날짜를 선택하면 저장된 기록이 없고 화면에서만 휴무로 표시한다는 안내를 보여준다.
- 월간 일별 목록은 기록 있는 날만이 아니라 과거 날짜 범위를 기준으로 보여주며, 빈 일/월은 정기휴무로 구분한다.
- 브라우저 스모크 검수에 `statsVirtualHoliday`를 추가했다.

검수 결과:
- `npm run check`: 통과, 도메인 테스트 46/46.
- `npm run build`: 통과.
- `node --check scripts/browser-smoke.mjs`: 통과.
- 브라우저 스모크 검수: 새 검수 프로필의 `411x762 / DPR 2.63`에서 통과.
- 정기휴무 검수 항목: `statsVirtualHoliday` 통과.

상태:
- 로컬 소스 구현과 검수 완료.
- GitHub Pages 배포는 아직 하지 않았다.

## 2026-06-07 기록 정정 재수정 보강

사용자 지시:
- 현장에서는 잘못 누르기, 무료/유료 오선택, 구역 오선택이 당연히 생긴다.
- 로그 화면은 보기 전용으로 유지하고, 정정은 별도 화면에서 반복 수정 가능해야 한다.
- 대체배송을 도우미로 바꿨다가 다시 무료/유료 또는 구역으로 바꾸는 역방향 검수가 필요하다.
- 저장 차단만 하면 현장 진행이 막히므로 복구 경로까지 같이 설계해야 한다.

코기 이해:
- 단발 전환 버튼은 부족하다. 완료 구역과 도우미 기록을 모두 `기록 정정` 목록에 남겨 재수정 가능하게 한다.
- 도우미 기록은 무료/유료/수량/시각을 다시 저장할 수 있고, 필요하면 대체배송/힐스테이트/미주/추가구역 기록으로 복구한다.
- 복구된 미주/힐스테이트 ID도 통계 비율에서 미주/힐스로 분류해야 한다.

수정 파일:
- `instruction.md`
- `src/app/main.ts`
- `scripts/browser-smoke.mjs`
- `todo.md`
- `progress.md`
- `unresolved.md`
- `changelog.md`

검수 계획:
- `npm run check`
- `npm run build`
- 브라우저 스모크 검수: 411x762 / DPR 2.63 기준
- 검수 루트: 완료 구역 -> 도우미 유료, 도우미 유료 -> 무료, 도우미 -> 대체배송/힐스테이트/미주 복구, 대형 수량 경고, 시간 역전 차단

검수 중 발견:
- 도우미 전환 시 새 도우미 이벤트 ID를 `timeline.at(-1)`로 연결하던 버그가 있었다.
- 타임라인은 시간순 정렬이라 새 도우미 이벤트가 중간 시각이면 마지막 이벤트가 아닐 수 있다.
- 이 때문에 전환 후 도우미 재수정 카드가 안 뜰 수 있었고, 명시 이벤트 ID를 생성해 연결하도록 고쳤다.

검수 결과:
- `npm run check`: 통과, domain tests 48/48.
- `npm run build`: 통과.
- 브라우저 스모크 검수: 411x762 / DPR 2.63, v15 후보 화면에서 전체 true.
- 통과 항목: 힐스 첫 구역 직접배송, 대체배송 직접배송, 추가구역 직접배송, 대체배송 -> 도우미 유료, 도우미 유료 -> 무료, 도우미 -> 대체배송/힐스테이트/미주 복구.

## 2026-06-07 기록 정정 선택형 반복 수정 보강

상태: 구현/검수 진행 중

코기 이해:
- v15 화면은 완료 구역을 도우미 무료/유료로 바꾸는 카드 목록이라 오빠가 원한 방향과 다르다.
- 필요한 것은 `수정할 기록 선택 -> 기록 불러오기 -> 한 기록을 계속 재수정`하는 관리 화면이다.
- 완료 구역도 구역 종류, 이름, 수량, 시간, 실패/추가를 다시 고칠 수 있어야 한다.
- 도우미 전환/무료/유료/구역 복구도 사라지는 단발 작업이 아니라 다시 선택해서 반복 수정 가능해야 한다.
- 검수는 라이브/핸드폰 저장소가 아니라 로컬 테스트 서버와 별도 브라우저 프로필에서 한다.

수정 파일:
- `src/app/main.ts`
- `scripts/browser-smoke.mjs`
- `src/app/version.ts`
- `todo.md`
- `progress.md`

검수 계획:
- `npm run check`
- `npm run build`
- 브라우저 스모크: 411x762 / DPR 2.63, 별도 프로필
- 일부러 틀린 흐름: 대체배송 -> 도우미 유료 -> 도우미 무료 -> 대체배송, 대체배송 -> 도우미 유료 -> 힐스테이트, 힐스테이트 -> 도우미 무료 -> 미주
- 추가 검수: 선택한 완료 구역을 구역 기록으로 유지한 채 구역명/수량/시간을 다시 저장하고 재수정 화면에 남는지 확인

검수 중 발견:
- v15 화면은 오빠 의도와 다르게 `완료 구역 -> 도우미 전환`만 제공했다.
- 도우미에서 구역으로 복구한 기록은 실제 시간과 구역 순서가 어긋나 재수정 검증에 걸릴 수 있었다.
- 정정 화면에서 이름/수량만 고치려 해도 기존 시간칸이 검증 대상이 되어 저장이 막힐 수 있었다.

수정 결과:
- 기록 정정을 선택형 반복 수정 화면으로 바꿨다.
- 복구된 구역은 실제 시작시간 기준으로 순서를 재정렬한다.
- 시간칸을 실제로 바꾸지 않은 경우에는 시간 검증과 시간 업데이트를 건드리지 않는다.
- `APP_VERSION / CACHE_VERSION`을 `0.2.15-repeatable-record-correction / v16`으로 올렸다.

검수 결과:
- `npm run check`: 통과, domain tests 48/48.
- `npm run build`: 통과.
- 브라우저 스모크 검수: 411x762 / DPR 2.63, 별도 Chrome 프로필, 전체 항목 true.
- 통과 항목: 도우미 무료/유료 전환, 도우미 -> 대체배송/힐스테이트/미주 복구, 완료 구역 반복 수정, 대형 수량 경고, 모바일 화면 기준.
