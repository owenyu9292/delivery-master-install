# 2026-06-07 source-of-truth 이관 후 TODO

- [ ] `current-source/` 격리 스냅샷의 보관/삭제 여부를 사용자 승인 후 결정한다.
- [ ] 필드 핫픽스는 `src/` 원본에 반영하고 빌드 결과로만 게시 파일을 갱신한다.
- [ ] 새 단일 repo 기준으로 hook/pre-commit 적용 범위를 재검토한다.
- [x] 핫픽스 대화창 인수인계는 `HOTFIX_HANDOFF.md` 기준으로만 진행한다.
- [x] 핫픽스 채널을 핸드폰 현장 접속용 초경량 채널로 운영한다는 규칙을 `instruction.md`와 `HOTFIX_HANDOFF.md`에 정규화한다.
- [x] 핫픽스 채널의 현장 긴급 수정 요청은 임시 차단/복구용 최소 수정, 검사, 빌드, 배포 전량 승인으로 간주한다고 명시한다.
- [x] 핫픽스에서 막은 내용은 문서화하고 정식 수정은 일반 작업창에서 진행한다고 명시한다.
- [x] 도우미 배송 무료/유료를 작업 화면에 추가한다. 무료는 총수량 포함/효율 제외, 유료는 총수량 포함/효율 포함으로 계산한다.
- [x] 완료 구역 정정 화면에서 대체배송/추가구역을 도우미 배송 무료/유료로 전환할 수 있게 한다.
- [x] 도우미 배송 수량이 2구역 이후 전체수량 차감 대상에 포함되는지 테스트한다.
- [ ] 도우미 배송 정정 UI를 갤럭시 폴드 앞 화면 기준으로 브라우저 검수한다.

# 배송마스터 개발앱 TODO

사용자 지시는 구현 전에 이 문서의 작업 항목으로 바꾼다.

## 진행 예정

- [ ] 필드 테스트 후 통계 화면에서 실제 장기 데이터가 많을 때 스크롤 양과 카드 순서가 현장 감각에 맞는지 확인한다.

## 보류

- [ ] 개발앱 필드 테스트 기간에 Google Drive 수동 백업을 운영 규칙으로 넣을지 결정한다.
- [ ] 당장 현장 흐름이 안정된 뒤 인코딩이 깨진 기존 문서를 복구한다.

## 완료

- [x] 배송마스터 세대/폴더 기준을 `instruction.md`와 `README.md`에 정리했다.
- [x] 공용 작업 트리/훅은 Hermes 서버 기준이 아니라 배송마스터 개발앱 PWA 기준으로 선별 적용하기로 정리했다.
- [x] 개발앱 원본용 프로젝트 맞춤 `.githooks/pre-commit` 초안을 작성했다.
- [x] 공용 hook을 그대로 설치하지 않고 배송마스터 세대/폴더 구조에 맞춘 dry-run 검사를 만들었다.
- [x] 로그에서 같은 분에 `이전 구역 완료`와 `다음 구역 시작`이 겹치면 이전 구역 완료를 먼저 보여주게 했다.
- [x] 브라우저 스모크 검수에 `previousZoneEndBeforeNextZoneStart` 항목을 추가했다.
- [x] 과거 일요일/월요일 빈 날짜를 저장 없이 화면에서만 `정기휴무`로 표시하게 했다.
- [x] 날짜조회에서 과거 정기휴무 빈 날짜를 선택하면 백업 JSON에 기록을 만들지 않는다는 안내를 보여주게 했다.
- [x] 브라우저 스모크 검수에 `statsVirtualHoliday` 항목을 추가했다.
- [x] 통계 탭을 현장앱 기준의 기간 탐색형 화면으로 보강했다.
- [x] 주간/월간 통계에서 미주/힐스테이트/대체배송 비율카드를 핵심 카드로 최상단에 배치했다.
- [x] 주간 통계에 이전주/다음주 이동, 주 범위 표시, 총 배송/근무일/평균 효율/스캔미스, 구역별 효율, 일별 현황을 추가했다.
- [x] 월간 통계에 이전달/다음달 이동, 월 범위 표시, 총 배송/근무일/평균 효율/스캔미스, 최다/최소 배송일, 일 평균 배송, 구역별 월간 효율, 요일별 평균을 추가했다.
- [x] 날짜별 조회를 추가해 특정 날짜의 로그와 리포트를 직접 열어볼 수 있게 했다.
- [x] 통계 화면도 실제 폴드 앞 화면 `411x762 / DPR 2.63` 기준으로 브라우저 검수했다.
- [x] 브라우저 검수는 데스크톱 전체 화면이 아니라 실제 핸드폰에 가까운 모바일 폭으로 진행한다는 규칙을 `instruction.md` 최상단 검수 원칙에 추가했다.
- [x] 모바일 검수 기준을 갤럭시 폴드 접힌 앞 화면 근사값 `360x800 / DPR 3`으로 보강했다.
- [x] 실제 폴드 앞 화면 CSS 값 `411x762 / DPR 2.63`을 기본 검수 기준으로 업데이트했다.
- [x] 오빠가 보내는 현장 스크린샷을 갤럭시 폴드 앞 화면 기준 검수 증거로 취급하도록 `instruction.md`에 추가했다.
- [x] `백업설정`에 실제 브라우저 CSS 화면값을 보여주는 `화면 정보` 항목을 추가했다.
- [x] 완료 기록 수정 폼의 날짜/연도 포함 입력을 시간 전용 입력으로 바꿨다.
- [x] 완료 기록 수정 폼이 모바일에서 잘리지 않도록 미주/일반 구역 전용 그리드와 입력칸 폭을 정리했다.
- [x] 완료 기록 수정 시 청량리 도착, 이전 구역 완료, 다음 구역 시작, 업무 종료와 충돌하는 시간 저장을 차단했다.
- [x] 로그 화면은 같은 분 안에서 업무 순서가 뒤집히지 않도록 업무 이벤트 우선순위로 정렬한다.
- [x] 모바일 폭 브라우저 검수에 완료 수정 폼, timeline 시간 차단, 로그 순서 검증을 추가했다.
- [x] 중요 지시가 하위 문서에 묻히지 않도록 `instruction.md`에 최상위 규칙 승격 기준을 추가했다.
- [x] 데이터 보존과 `DayRecord.timeline` 원본 우선 원칙을 `instruction.md` 최상단 원칙으로 승격했다.
- [x] 실제 앱/브라우저 검수 없이는 현장 흐름 작업을 완료 처리하지 않는 원칙을 `instruction.md` 최상단 원칙으로 승격했다.
- [x] 작업 전 절차에 새 지시가 최상위 규칙 후보인지 판단하는 단계를 추가했다.
- [x] 사용자 편의 / 현장 다음 행동 우선 원칙을 `instruction.md` 최상단 0번 항목으로 승격했다.
- [x] 한국어 문서를 처음부터 UTF-8 기준 도구로 확인하도록 문서 인코딩 규칙을 강화했다.
- [x] 한국어 문서 UTF-8 우선 규칙을 `instruction.md` 최상단 작업 전제로 승격하고 작업 전 확인 절차에 추가했다.
- [x] 힐스테이트/대체배송/추가구역 공통 UI에서 다음 행동을 최상단 주버튼으로 배치했다.
- [x] 공통 구역 UI에서 `정리 시작`을 기본 주버튼, `바로 배송 시작`을 보조 버튼으로 분리했다.
- [x] 정리 중 화면은 `정리 완료`를 안내문보다 먼저 보여주게 했다.
- [x] 수량 입력 단계는 `전체 수량` 입력과 완료 버튼을 설명보다 먼저 보여주게 했다.
- [x] 검수 기준에 공통 구역 UI 순서 검증을 추가했다.
- [x] 출발 예상 수량 빈칸을 0으로 처리하지 않고 차단했다.
- [x] 무보수 도우미날은 사용자가 `0`을 직접 입력하고 확인해야 시작되게 했다.
- [x] 청량리 도착 후 기본 구역 순서를 자동 생성하게 했다.
- [x] 현재 구역 시작 버튼을 작업 순서 편집 UI보다 위에 배치했다.
- [x] 오늘 초기화와 완료 기록 수정 전에 실제 백업 JSON을 다운로드하게 했다.
- [x] 업무 종료 시 전체 개발앱 백업 JSON 다운로드를 자동으로 시작하게 했다.
- [x] 완료 기록 수정에서 시간 역전을 차단했다.
- [x] 정리 시작/정리 완료 중복 이벤트를 방어했다.
- [x] 뒤 구역 전체 수량 자동차감 조건을 당일 총량 근처 입력으로 제한했다.
- [x] 현장앱 import 미주 A/B 상세값을 리포트까지 이어지게 보정했다.
- [x] 브라우저 스모크 검수에 빈 출발 수량 차단과 현재 구역 시작 버튼 위치 검증을 추가했다.
- [x] 앱 상단과 백업설정에 `v0.2.9-field-log-report · 2026-05-29 수정본` 표시를 추가했다.
- [x] 뒤 구역에 당일 전체 수량을 입력했을 때 확인창 없이 자동 차감해 저장한다.
- [x] 현장앱 기준의 로그 탭을 개발앱에 구현했다.
- [x] 로그만 봐도 출발, 도착, 구역 시작, 미주 상세, 정리 시작/완료, 구역 완료, 업무 종료를 시간순으로 복구할 수 있게 했다.
- [x] 리포트를 현장앱 포맷 기준으로 재구성하고 출발/도착/운전시간/최종 종료를 포함했다.
- [x] 리포트의 구역별 상세에 시작~종료, 실제 배송 소요, 정리 시간, 실제/전체/이벤트 보정 효율을 표시했다.
- [x] 주간/월간 비율 배지의 `557:963:25` 같은 원시 표기를 사람이 읽기 좋은 표현으로 바꿨다.
- [x] 화면 탭을 `업무 / 로그 / 리포트 / 통계 / 백업설정` 기준으로 정리했다.
- [x] `백업설정` 탭에 백업 내보내기, 현장앱 백업 가져오기, 오늘 초기화, 버전 확인을 모았다.
- [x] 브라우저 검수에서 전체 수량 자동차감 확인창 제거, 로그/리포트/통계/백업설정 탭 표시를 확인했다.
- [x] 현장 입력 UX 1차 개편을 브랜치에서 진행했다.
- [x] 작업 순서를 미주/힐스테이트/대체배송/추가구역 기본 목록과 화살표 조정 방식으로 바꿨다.
- [x] 미주 입력은 1동/2동/3동을 먼저 기록하고, 전체 수량 입력으로 나머지를 자동 계산하게 바꿨다.
- [x] 완료 기록 수정에서도 미주 나머지 수량을 사용자가 직접 계산하지 않게 바꿨다.
- [x] 힐스테이트/대체배송/추가구역 흐름을 정리 시작, 정리 완료, 바로 배송 시작 중심으로 줄였다.
- [x] 리포트 시간 표시는 `n시간 n분` 형식으로 정리했다.
- [x] 업무 종료 후 저장 완료 피드백을 보여주게 했다.
- [x] 무입력 완료와 말도 안 되게 큰 수량 입력을 실제 브라우저 검수 시나리오에 넣었다.
- [x] 필수 운영 문서 `instruction.md`, `todo.md`, `progress.md`, `unresolved.md`, `changelog.md`를 만들었다.
- [x] 운영 문서 내용을 한국어 중심으로 정리하고 UTF-8/문서 깨짐 규칙을 추가했다.
- [x] `instruction.md` 7번을 실제 앱/브라우저 검수 규칙으로 정리하고, 현재 문제 목록은 장부 문서로 분리했다.
- [x] 새 지시를 받으면 먼저 이해 내용을 보고하고 승인 후 작업한다는 규칙을 문서에 추가했다.
- [x] 미주/힐스 개별 증상을 전 구역 공통 입력 안정성 문제로 승격해 정리했다.
- [x] 전 구역 완료 저장 전 공통 입력 검증 레이어를 만들었다.
- [x] 전 구역에서 부분 입력, 필수값 누락, 시간 누락, 정리시간 누락을 완료 전에 차단하거나 강하게 확인한다.
- [x] 전 구역에서 전체 수량 입력 시 이미 입력된 구역 수량을 빼서 현재 구역 수량을 자동 계산하는 흐름을 만들었다.
- [x] 전 구역에서 너무 크거나 작은 수량, 예상 수량/당일 총량/완료 구역 합계와 충돌하는 수량을 경고 또는 차단한다.
- [x] 미주처럼 상세 수량이 있는 구역은 상세 합계와 총합 관계를 공통 규칙 위에서 검증한다.
- [x] 실패/추가 수량칸을 모바일 주 수량 입력줄에서 분리했다.
- [x] 미주/힐스테이트/대체배송지/추가구역 작업 순서를 화살표로 위아래 조정할 수 있게 했다.
- [x] 앱 상단은 `v10`처럼 짧게 표시하고, 백업설정에는 상세 버전/수정일/cache 버전을 표시하도록 정리했다.
- [x] 앱 표시 버전과 서비스워커 캐시 버전의 기준을 `src/app/version.ts`로 묶고 빌드 때 `dist/sw.js`에 자동 반영되게 했다.
- [x] 배포 전 서비스워커 캐시 버전과 앱 버전을 `v11 / 0.2.10-field-fold-cover`로 올렸다.
- [x] 배포 저장소에 반영하고 GitHub Pages 원격 `sw.js`에서 `delivery-master-install-v11`을 확인했다.
## 2026-06-03 작업 트리 / 훅 보강

- [x] 승인 직후 `progress.md`에 작업 시작 기록을 남기는 규칙을 `instruction.md`에 추가한다.
- [x] 코드/스크립트 변경 커밋 시 `progress.md`와 `changelog.md`가 함께 staged 되도록 훅을 보강한다.
- [x] 훅 보강 후 기본 문법 검사와 dry-run 검사를 실행한다.
## 2026-06-06 정리 UI / 통계 비율 최종 보정

- [x] 정리 완료 누락 보정 패널에서 `정리 완료`를 최상위 주버튼으로 배치한다.
- [x] 주간/월간 통계 비율 배지를 수량비에서 백분율 요약으로 바꾼다.
- [x] 브라우저 검수에 정리 보정 버튼 순서와 백분율 배지 확인을 추가한다.
- [x] 검사, 빌드, 배포를 완료한다.
## 2026-06-07 바로 배송 시작 완료 저장 핫픽스

- [x] 미주 외 구역에서 `바로 배송 시작` 루트는 `정리 완료` 없이 완료 저장되게 한다.
- [x] 힐스 첫 구역 `바로 배송 시작 -> 13개 완료` 검수 케이스를 추가한다.
- [x] v13으로 빌드하고 배포한다.
## 2026-06-07 직접배송 공통 검수 확장

- [x] 현장 장애 긴급 대응 기준을 `instruction.md` 최상위 규칙에 추가한다.
- [x] 대체배송 `바로 배송 시작 -> 완료` 검수 케이스를 추가한다.
- [x] 추가구역 `바로 배송 시작 -> 완료` 검수 케이스를 추가한다.
## 2026-06-07 기록 정정 재수정 보강

- [x] 완료 구역을 도우미 배송 무료/유료로 전환한 뒤에도 다시 수정할 수 있게 한다.
- [x] 도우미 배송 무료/유료를 서로 바꾸고 수량/시각을 재수정할 수 있게 한다.
- [x] 잘못 전환한 도우미 배송을 대체배송/힐스테이트/미주/추가구역 기록으로 되돌릴 수 있게 한다.
- [x] 복구된 미주/힐스테이트 구역도 주간/월간 비율 통계에서 올바른 분류로 잡히게 한다.
- [x] 브라우저 스모크 검수에 `대체배송 -> 도우미 유료 -> 도우미 무료 -> 대체배송`, `도우미 -> 힐스테이트`, `도우미 -> 미주` 역변환 루트를 추가한다.
- [x] 기록 정정을 단발 전환 카드가 아니라 `수정할 기록 선택 -> 기록 불러오기 -> 선택 기록 정정 반영` 흐름으로 바꾼다.
- [x] 선택한 완료 구역의 구역 종류, 구역 이름, 수량, 시작/종료, 정리 시작/완료, 실패/추가를 같은 화면에서 반복 수정할 수 있게 한다.
- [ ] 시간 1분/역전/과도 수량 같은 비정상 필드 상황은 다음 검수 보강에서 별도 시나리오로 더 넓힌다.
