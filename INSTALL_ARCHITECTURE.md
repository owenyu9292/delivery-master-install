# 배송마스터 Android 설치판 구조

## 1. 현재 기준

- 현재 개발앱 PWA의 source-of-truth는 이 저장소의 `src/`다.
- 개발앱 PWA는 설치판 제작 이후에도 원본 코드와 긴급 복구 기준으로 유지한다.
- 기존 현장앱 v1과 season2는 보관/백업용이며 설치판 구현 대상으로 사용하지 않는다.
- 설치판은 별도 복제 앱이 아니라 같은 도메인/UI 소스를 Android 실행 환경에 연결한 배포 형태다.

## 2. 채택 방향

- Android 설치판의 1차 기술 방향은 Capacitor다.
- TWA는 웹 origin과 브라우저 저장소 의존을 유지하므로 앱 내부 갱신 저장이라는 최종 목적에 사용하지 않는다.
- Electron/Tauri 데스크톱 패키징은 Galaxy Fold 현장앱 목적에서 제외한다.
- Capacitor 의존성과 Android 프로젝트를 생성했으며 package ID는 `io.github.owenyu9292.deliverymaster`다.

## 3. 원본과 설치판의 관계

```text
개발앱 PWA 원본(src/domain, src/ui, src/app)
  -> 공통 빌드
     -> GitHub Pages PWA
     -> Capacitor Android 설치판
```

- 계산, `DayRecord.timeline`, 리포트, 통계는 공통 소스를 사용한다.
- Android 전용 코드는 저장소, 파일 내보내기, 업데이트, 뒤로가기 같은 플랫폼 경계에만 둔다.
- PWA와 설치판이 업무 로직을 복사해 따로 발전하게 만들지 않는다.
- 설치판에서 발견한 공통 버그는 공통 원본에 반영한 뒤 PWA와 설치판을 각각 검증한다.

## 4. 내부 저장

- 설치판에서도 업무 원본은 `DayRecord.timeline`이다.
- 날짜를 키로 한 `upsert`를 사용해 같은 날짜 기록을 갱신한다.
- `DayStore` 인터페이스를 유지하고 Android 내부 저장 어댑터를 추가한다.
- 내부 SQLite를 채택했고 날짜 키와 `DayRecord` JSON 본문을 저장하는 `SqliteDayStore`를 구현했다.
- 리포트, 로그, 통계, 화면 캐시는 저장 원본으로 중복 보관하지 않는다.
- 앱 버전과 데이터 스키마 버전을 분리한다.

## 5. PWA 데이터 최초 이전

- PWA IndexedDB와 Android 내부 저장소는 자동 공유되지 않는다고 전제한다.
- 설치 전 PWA에서 전체 개발앱 백업 JSON을 만든다.
- 최초 실행에서 `백업 선택 -> 미리보기 -> 날짜/건수 확인 -> 가져오기` 순서로 한 번 이전한다.
- 날짜 충돌은 자동 덮어쓰지 않고 복사 또는 명시적 덮어쓰기만 허용한다.
- 이전 후 날짜 수, timeline 이벤트 수, 구역 수량, 종료 상태, 주간/월간 통계를 비교한다.
- 검증이 끝날 때까지 PWA 원본과 이전용 JSON을 삭제하지 않는다.

## 6. 백업과 복구

- 정상 저장은 내부 저장소 갱신으로 끝내며 매 저장마다 다운로드 파일을 만들지 않는다.
- 업무 종료 시 내부 최신 백업 스냅샷을 갱신한다.
- 외부 파일 내보내기는 기기 교체와 재설치 복구용으로 유지한다.
- 브라우저 `<a download>` 대신 Android 파일 저장/공유 기능을 사용한다.
- 앱 삭제 시 내부 자료가 삭제될 수 있으므로 외부 백업 없이 완전 안전하다고 표시하지 않는다.
- 초기화, 덮어쓰기, 스키마 이전 전에는 자동 스냅샷을 만든다.

## 7. 업데이트와 서명

- Android package ID와 서명키는 첫 설치판부터 고정한다.
- 같은 package ID와 서명키로 업데이트해야 내부 데이터가 유지된다.
- PWA 캐시 버전과 Android 앱 버전은 별도로 관리한다.
- 설치판에서는 서비스워커 강제 갱신을 업데이트 수단으로 사용하지 않는다.
- 업데이트 전 백업과 업데이트 후 DB/날짜목록/당일기록/리포트를 확인한다.

## 8. 완료 게이트

- 공통 검사와 빌드 통과
- Android 패키지 빌드와 서명 확인
- Galaxy Fold 앞 화면 설치/실행 검수
- PWA 백업 최초 가져오기 검수
- 앱 종료, 재실행, 기기 재부팅 후 기록 유지
- 오프라인 입력, 조회, 리포트, 통계 확인
- 동일 서명 업데이트 후 기록 유지
- 잘못된 백업, 중복 날짜, 구버전 스키마의 복구 경로 확인
- 앱 삭제/재설치 후 외부 백업 복구 확인
- 업무 종료 후 중복 다운로드 파일이 생기지 않는지 확인

## 9. 확정 및 미결정 항목

- Android package ID: `io.github.owenyu9292.deliverymaster` 확정
- 서명키 생성 및 이중 백업 위치
- SQLite 플러그인의 SQLCipher 포함에 따른 배포 방식별 수출 규정 확인
- APK 직접 배포 또는 Play 내부 테스트
- 내부 백업 보존 개수와 회전 규칙
- PWA/설치판 병행 기간의 데이터 합치기 정책
