# 2026-09-05 인스톨 앱 데이터 보존·정정 검수

## 대상과 상태

- 기록자: 일반 작업창(main). 루나 2명은 저장소/계산을 분담했고 코기가 코드를 재검토하고 통합·브라우저 검수를 수행했다.
- 대상 repo: C:\Codex55Workspace\delivery-master\delivery-master-install-deploy
- 브랜치: codex/hils-handling-minutes / 기준 커밋: 35af4fc
- 로컬 소스 수정·검수 완료. 커밋·푸시·APK 패키징·휴대폰 설치·GitHub 배포는 미실행.
- 실제 휴대폰 기록 및 기존 PWA/season2 게시본을 테스트에 사용하거나 변경하지 않았다.

## 반영한 내용

1. 힐스 반품·선집화·상차 소요분: 기본 제안30, 직접0~999분, 반복 기록은 같은 건 갱신. 기존 정리에 추가 차감하고 전체 경과/수량은 유지한다.
2. 백업 내부 timeline/zone/helper/adjustment 구조, 중복 ID, 잘못된 숫자 구조를 쓰기 전에 검사한다. 호환 필드와 의도적인 시간 역전 정정은 보존한다.
3. 다중 날짜 복구는 SQLite/IndexedDB에서 전체 성공 또는 전체 취소한다. 두 번째 put의 동기 예외에도 첫 번째 날짜를 남기지 않는다.
4. 가져오기 반영 후 후속 스냅샷만 실패하면 실제 반영 건수를 표시한다. 반영0건으로 오보하지 않는다.
5. 손상된 과거 기록이 있어도 정상 날짜를 열 수 있다. 오늘 기록을 읽지 못해도 빈 기록으로 덮지 않고 안전 보관/백업 복구를 제공한다.
6. SQLite JSON 원문 자체가 손상됐거나 null이면 날짜와 rawCorruptJson을 corruptDays에 분리 보관한다. 이를 정상 업무 기록처럼 계산하지 않는다. 복구에는 유효한 백업이 필요하다.
7. 탭 이동·새로고침 시 미저장 입력 복원, 저장 실패 후 기록/편집칸 유지, 재시도, 중복 탭 보호를 반영했다.
8. 무료 구역 도우미는 효율 분자에서 제외, 유료는 유지한다. 미주 총량 정정은 기존 A를 유지하며 B 및 이전 별칭을 재계산한다.
9. 누락 정리 완료 정정 시 자동 생성 배송시작도 재계산한다. 수동으로 고친 배송시작은 자동 보정으로 덮지 않는다.
10. 시간 역전/확인 불가/1분 미만 배송은 효율을 미확정으로 표시한다. 잘못된 시간을 다시 수정할 수 있고 수량은 삭제하지 않는다.
11. 통계 효율은 수량/시간 가중 계산한다. 리포트의 대체배송 제외 정규 효율도 실제 제외 구역으로 계산한다.
12. 업무종료 로그 시간 수정, 진행한 구역은 유지한 채 남은 구역 순서 변경, 자정의 이전 업무 계속/오늘 업무 선택을 추가했다.
13. 같은 분 로그는 도착→해당 구역 시작→정리→배송→완료 순서, 이전 구역 완료→다음 구역 시작으로 표시한다. 원본 시각은 변경하지 않는다.
14. 로그 연필48px, 주/월 이동 가로 배치, 비율 카드 앞 장황한 안내 제거를 적용했다.

## 검수 증거

- npm run check: 통과. 타입 검사, 기존 도메인61/61, 플랫폼, SQLite, runtime, handling, storage-safety, sqlite-file, correction-safety.
- npm run check:handling: 최종22개 시나리오 묶음 통과. Chrome headless, 임시 프로필/별도 localhost/임시 빌드만 사용.
- 브라우저 화면: 411x762 DPR2.63 및360폭. 가로 넘침 없음, 로그 수정 버튼 최소48x48. 스크린샷 직접 확인.
- HANDLING_FULL_REGRESSION=1: 기존 browser-smoke와 대체배송10개 경로 통과. 이후 변경은 SQLite 손상원문 보관 보강 및 통계 안내 문구 제거이며 최종 자동검수/22개 브라우저 검사를 다시 수행했다.
- 최종 브라우저: C:\Users\Lenovo\AppData\Local\Temp\delivery-handling-pwGCpW\result.json
- 전체 경로 회귀: C:\Users\Lenovo\AppData\Local\Temp\delivery-handling-xPFcsg\result.json
- 실제 SQLite 파일 검사: C:\Users\Lenovo\AppData\Local\Temp\delivery-sqlite-safety-6Q6nPX\records.sqlite
- 재실행: scripts/browser-handling.mjs, scripts/browser-safety-cases.mjs, scripts/run-native-sqlite-safety-tests.ts.

## 고의 실패와 복구

- 빈값/음수/소수/문자/1000/Infinity 작업시간은 저장 거부, 기존30분 유지.
- 저장 실패 뒤 입력123 및 하루 기록 전체 불변, 재시도·두 번 누름 후 이벤트1건.
- 로그 저장 실패 뒤120개 입력 유지, 재시작 후 재수정 성공.
- 완료를 출발보다 전인07:00로 수정: 수량 보존·효율 미확정.11:00 재정정 후 정상 효율 복귀.
- 정리 완료 누락09:30 추가: 자동 배송시작09:30 재연결, 배송90분 복구.
- 손상 백업 [null] 차단, IndexedDB 두 번째 쓰기 동기 예외/트랜잭션 abort 시 원 날짜 전체 불변.
- 가져오기 뒤 두 번째 스냅샷 실패: 실제2일 반영을 표시. 기존날짜 보존 선택은0일 반영 및 기존120개 유지.
- 손상 과거 날짜 보존/타 날짜 사용/선택 복구, 손상 오늘 기록의 초기화 없는 복구.
- 자정 전 업무 기록 전체 불변, 오늘 열기 시 다른 날짜 기록으로 생성.
- 실제 SQLite 엔진: DB trigger로 두 번째 쓰기 중단, 재개방 시 원본 유지, 성공 복구 재개방 유지, 미커밋 연결 종료 rollback, 깨진 JSON/null 원문 보관, 선택 날짜만 복구.
- 검수 중 발견한 put 동기 예외의 부분 커밋 위험과 같은 분 로그 순서 오류를 추가 수정했다. 과거 UI 제목을 기대한 테스트는 실제 '남은 작업 순서' 명칭으로 갱신했다. 오류 한국어 문구 계약은 유지했다.

## 배포 후속 및 남은 실기기 확인

- 후속 승인으로 v35 빌드·배포 및 공개 다운로드 해시 확인 완료. 소스/APK 커밋541eaf0. 상세 증거: VERIFICATION_v35.md. 위 미배포 상태는 로컬 검수 당시 기록이다.

- 위 SQLite 파일 검사는 실제 엔진 검증이지만 Android Capacitor 플러그인/실기기 검증은 아니다.
- 새 APK에서 Fold7 기존 자료 백업→같은 package ID/서명 업데이트 설치→SQLite 재조회→파일 내보내기/가져오기→앱 종료·재실행을 확인해야 한다.
- 새 버전·APK 해시·GitHub Release는 VERIFICATION_v35.md에 기록했다. 휴대폰은 미연결이므로 실제 설치된 v34에 이 수정이 반영됐다고 안내하면 안 된다.
- 장치 저장공간 자체 손상/앱 삭제는 이번 코드로 복구를 보장할 수 없다. 외부 전체 백업을 별도로 유지해야 한다.
