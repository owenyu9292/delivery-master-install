# 통계 확장 및 앱 전체 재검수

## 현재 상태

- 2026-09-09, 기록자 main. 인스톨판 소스 구현·로컬 빌드·검수 완료. 이번 변경은 아직 커밋·푸시·APK 배포하지 않았다.
- 기준 커밋 75b317f, 브랜치 codex/hils-handling-minutes. 배포된 앱은 v37이며 사용자는 설치했다고 알렸다. 이번 통계 확장은 설치본에 아직 없다.
- PWA 게시본, main, season2, 실사용 휴대폰 자료 수정 없음. 통계는 원본 DayRecord에서 읽기 전용으로 파생하며 저장 형식이나 수량 입력 로직을 바꾸지 않는다.

## 구현

- 최상단 구역 비율, 이전 같은 경과 기간 비교, 최근 6주/6개월 수량 그래프, 업무 시간 구성.
- 펼침 상세: 비율 변화, 물량별 효율, 최다·최소 물량, 구역별/요일별 실적, 도우미 이용일 비교, 미주 A/B, 평균 출발·마감, 스캔 차이·실패·추가, 확인이 필요한 기록과 날짜 조회.
- 기존 주/월 과거 이동 및 날짜별 조회 유지. 시스템 폰트 유지. 비율은 구역 수량 기준이며 별도 도우미 수량은 따로 표시한다.
- 마감 자료만 집계. 진행 중, 복사 백업, 잘못된 연결/수량 자료는 구분한다. 같은 날짜가 중복된 입력은 최신 갱신 기록 한 건만 집계한다.
- 예상 수량이 있는 날만 스캔 차이 계산. 순차와 가중 효율을 사용하며 누락 시간을 확정값으로 표시하지 않는다. 기존 기록의 배송 시작 대체 계산은 자동 보정 포함으로 표시한다.
- 하루 1200개/구역 800개 초과, 시간당 600개 초과, 시간 역전, 도우미 수량의 구역 초과 등은 수량을 보존하되 효율 표본에서 제외하고 확인 항목을 표시한다. 업무 입력이나 저장을 막는 새 규칙이 아니다.
- 손상 수량/참조는 집계 제외 사유를 표시하며 자동으로 원본을 고치지 않는다. 시간 구성이 중복되면 구성 표본에서 제외하고, 분류 근거 없는 시간은 미분류로 표시한다.

## 실제 검수

1. `npm run check` 통과: 도메인61, 플랫폼/런타임/SQLite/저장·복구/정정/구역 ID, 시간 연동17묶음, 자동 정리18, 시간·수량 교차2684조합, 도우미 원복11묶음, 통계15묶음+600변형.
2. `npm run check:browser` 두 차례 통과. 마지막 전체 실행은 업무·시간·도우미·통계26묶음과 저장·정정22묶음. 사용자의 브라우저와 별도 프로필/HTTP origin/임시 DB를 사용한 headless Chrome.
3. 마지막 소규모 표시 보완 후 `STATS_ONLY=1 node scripts/browser-field-workflow.mjs` 통과. 401일 자료, 월간18회 뒤로/앞으로, 상세 전부 펼치기, 날짜 조회, 탭 왕복 후 원본401건 완전 일치.
4. Fold7 앞 화면 411x762 / DPR2.63, 추가360폭, 글자150%에서 화면 검수. 스크린샷 직접 확인. 오류/NaN/Infinity/가로 넘침/검사 대상 텍스트 잘림 없음.
5. `npm run build`, `git diff --check` 통과. 생성 dist는 인스톨 저장소 내부만 재생성.

전체 브라우저에는 무입력/0/99999 후 복구, 반복 대체배송/미주/힐스 전환, 구역 순번 변경, 도우미 유료→무료→구역 재복원, 연속 누름, 시간 역전→정정, 정리 완료 누락→자동/수동 보완, 자정 편집, 저장 실패/느린 저장/재시도, 손상 백업, 가져오기 두 번째 쓰기 실패/전체 롤백, 덮어쓰기/건너뛰기, 손상 자료 보존 케이스가 포함된다.

## 증거 경로

- 전체 업무/통계: `C:\Users\Lenovo\AppData\Local\Temp\delivery-field-v37-AYILbZ\result.json`
- 저장/정정: `C:\Users\Lenovo\AppData\Local\Temp\delivery-handling-rH2u4f\result.json`
- 최종 통계 재검수 및 스크린샷: `C:\Users\Lenovo\AppData\Local\Temp\delivery-field-v37-w2ZjTM\`
- 실제 SQLite 파일6종: `C:\Users\Lenovo\AppData\Local\Temp\delivery-sqlite-safety-ZuQXsv\`

## 남은 확인

- 이번 통계판 APK의 실기기 검수와 배포는 미실행. 브라우저 통과를 Android 네이티브 파일 선택/공유/SQLite 플러그인 검수로 표현하지 않는다.
- 실제 Fold7 유료 폰트, 키보드, 절전 후 복귀, 설치 업데이트와 네이티브 파일 왕복은 별도 확인이 필요하다. 기존 자료를 삭제하거나 테스트용으로 바꾸지 않는다.
- 모든 가능한 현장 오류가 없다는 보장은 아니다. 발견되지 않은 위험과 실기기 미확인 항목을 구분한다.

## 변경 파일

`src/analytics/statistics.ts`, `src/ui/statisticsView.ts`, `src/app/main.ts`, `public/field-app.css`, `scripts/run-statistics-tests.ts`, `scripts/browser-statistics-cases.mjs`, `scripts/browser-field-workflow.mjs`, `package.json` 및 이번 작업 기록.
