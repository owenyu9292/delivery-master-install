# v39 기본 재검수 - 2026-09-09

## 판정과 범위

- 초기 v39를 폰에 덮어 설치했다. 실제 폰에서 출발 입력과 버튼 사이 0px 간격이 추가 발견되어 간격 보완 및 재검수 중이다. 최종 완료 판정은 보류한다.
- 공개 배포, 커밋, 푸시는 하지 않았다. PWA 원본은 변경하지 않았다. 실사용 자료는 백업 후 업데이트 보존을 확인했다.
- 브라우저/정적 검사 통과를 실기기 통과 또는 모든 버그 제거로 표현하지 않는다.

## 새로 재현하고 수정한 결함

1. 잘못된 숫자의 의미 변경: 로그 수량 -1 입력이 1로 바뀌는 실제 브라우저 실패 재현. 시간/수량 공통으로 기호 제거와 자릿수 절단을 없앴다. 음수, 소수, 지수, 문자, 초과 길이는 그대로 보여주고 해당 저장만 거절한다. 올바른 값으로 즉시 수정·저장할 수 있다.
2. 저장 전 성공 알림: 저장 Promise를 지연시킨 상태에서 '저장했습니다' 표시 재현. 로그/도우미/완료 정정 및 동별 기록 알림을 저장 성공 뒤로 옮겼다. 실패 시 원본과 편집값101 보존 후 재시도 성공 확인.
3. 동별 기록 지우기 실패의 화면 유실: DB 저장을 실패시키면 원본은 남으나 화면11/22/33이 빈칸이 되는 재현. 해당 초안은 저장 성공 뒤에만 지운다. 실패 시 세 수량 보존, 성공 시 해당 값만 삭제하고 누적90 초안은 재시작 후에도 유지한다.

수정 파일: src/app/main.ts, src/app/numericInput.ts.
기존 로컬 스크롤/뒤로가기/리포트/키보드 수정도 모두 포함한다.

## 실행한 검수

| 영역 | 실제 증거 | 판정 |
| --- | --- | --- |
| 잘못된 입력 | 공통 파서106조건, 로그 실제 음수/소수/문자/긴 값/시간 범위와 재수정 | 통과 |
| 입력 화면 | 출발 전, 힐스/미주 작업, 모든 핵심 로그 편집, 백업/통계. 3크기168회 경계·히트검사 | 통과 |
| 기본 조작 | 다음 입력 이동, 탭6회 왕복, 새로고침, 회전/크기/복귀 이벤트12회, 초안/원본 불변 | 통과 |
| 스크롤·뒤로가기 | 5탭/4크기/3하단여백60조합, 마지막 메뉴 실제 클릭, 편집/탭/루트/저장 중 Back·Escape | 통과 |
| 업무·정정 | 반복 대체구역/순서 변경/도우미 무료·유료 왕복/수량/자동 정리/시간 연동/자정 | 통과 |
| 저장·복구 | 지연/실패/중복 누름/재시작, 다일 덮어쓰기 원자성, 손상 자료 원문 보존/복원, 취소 | 통과 |
| 계산 | 도메인64, 시간2684조합, 통계15묶음+600변형, 도우미11묶음 등 npm run check 전체 | 통과 |
| 실제 SQLite 파일 | 트리거 실패 롤백, 닫고 재열기, 미커밋 중단, 손상 원문 보존. Node SQLite 엔진 | 통과 |
| 키보드 화면 모사 | 수량/시/분/이름, 3개 가용 높이와 닫기16조건. 실제 Samsung IME는 아님 | 통과 |
| Android | assembleDebug 및 lintDebug, APK 내장4파일 일치, 패키지/버전/서명 유지 | 통과 |
| 실제 Fold7 | 기존 SQLite 152일 백업 후 초기 v39 덮어 설치, DB 해시 동일. 격리 QA 앱에서 삼성 숫자 키보드의 큰 공백 해소 확인. 시간 키보드/시스템 Back/문서 왕복 전체는 미완료 | 부분 확인 |

- 전체 최종 브라우저: 업무/기본48묶음 + 저장22묶음 + 키보드16조건. 168회 입력 경계 검사는48묶음에 포함, 중복 합산하지 않는다.
- 검사 방식의 한계: 일부 앱 내부 API/저장 실패는 테스트 주입. DOM 포커스/화면 크기/히트검사는 실제 Android IME 또는 물리 터치가 아니다. 파일 선택 플랫폼 계약 검사는 모의 응답이며 실제 Android 문서 제공자 왕복은 폰에서 별도 확인한다.
- 배경 Chrome은 전용 임시 프로필과 로컬 격리 origin 사용. 사용자의 기존 Chrome/자료를 초기화하지 않았다.
- 시간 분 입력의 작은 가용 화면 스크린샷을 직접 확인했다. 입력과 저장은 메뉴 위에서 보인다.

## Android 경고와 유지보수

- lintDebug: 오류0, 경고17. 권한 태그 순서1, Gradle/AppCompat 새 버전2, 불필요v24리소스1, 미사용 리소스8, 단색아이콘2, splash 크기/중복/위치3.
- 위 경고는 남아 있으며 '경고0'으로 보고하지 않는다. 동작 안정화 중 불필요한 라이브러리 일괄 업그레이드나 리소스 삭제는 하지 않았다.
- 최초 offline lint는 검사 라이브러리 캐시 부재로 실패했고, 프로젝트에 이미 선언된 의존성을 받은 후 정상 완료했다.
- 기본 실패 재현 검사는 scripts/browser-basic-audit.mjs, 전체 입력 표면 검사는 scripts/browser-input-surfaces.mjs. 기본 check:browser에 연결했다.
- 공통 숫자106조건은 기본 npm run check에 포함한다.

## 최초 설치본 (간격 보완 전)

- UI v39 / Android versionCode12 / versionName0.2.38.
- 패키지 io.github.owenyu9292.deliverymaster, 기존 서명 유지.
- APK: android/app/build/outputs/apk/debug/app-debug.apk.
- SHA-256: E1BDD65DF6C7FE4717661C6FD0A283C6AD3E357BBD8AC92D07D9BD349C00CDE2
- assets/app.js: CECCC213D3F9615CCF4DED8C2F881CD7D676243D27590A73029C37722E7A9650
- 인증서 SHA-256: f7f90c90211f12d16e03ce0381657690b5bd7de06b52f46abad70a795081f164
- 전체 동작 검수 후 버전 표기만 갱신했고 Android 빌드/lint 및 내장 파일 대조를 다시 수행했다.
- v39 최종 native-layout 분기에서도 키보드16조건/기본9묶음/입력168회 재검사 통과: delivery-field-v37-dNY0j2.
- 위 APK 해시는 간격 보완 전 실제 설치본이다. 후속 빌드의 최종 해시는 간격 검수 기록을 기준으로 확인한다.

## 증거와 연결 후 절차

- 최종 전체: delivery-field-v37-esyjmN, delivery-handling-gBopnW, delivery-field-v37-yjgEiu.
- 실제 SQLite 파일: delivery-sqlite-safety-jO05Dn.
- 저장된 증거: docs/review-basic-2026-09-09/의 결과 JSON, 스크린샷, lint XML.
- 폰 연결 후 설치된 패키지/서명/버전을 확인하고, 가능한 데이터 사본을 먼저 확보한다. 앱 삭제나 pm clear는 하지 않는다.
- 동일 패키지 덮어 설치 후 기존 날짜/수량/통계를 대조한다. 시험 기록은 실사용 날짜에 입력하지 않고 격리된 테스트 앱/자료에서 검증한다.
- 실제 키보드 열기/닫기/다음 입력/저장 버튼 도달, 끝 스크롤, Back/재실행, 시간 정정 및 파일 왕복을 확인한 뒤 실기기 결과를 별도로 기록한다.
- 사용자 연결 통보 후 Windows는 SAMSUNG Mobile USB Composite Device/Modem을 정상 인식했으나 adb devices -l은 두 차례 빈 목록이었다. 디버깅 연결 전이므로 설치와 실기기 검수를 시작하지 않았으며 USB 디버깅 확인을 안내했다.

## 연결 후 확인 및 추가 결함

- 이후 ADB 연결 성공. 사용자 명시 승인 후 .device-backup/v39-before/app-data.tar에 앱 사본을 보관했다. 사본은 Git 제외이며 외부 업로드하지 않는다.
- 실사용 DB 152일, 최초 날짜 2026-02-24 / 마지막 날짜 2026-09-09. 파일 SHA-256 454064351457189F98921C4549F3B462E894FAB9D057149190196358BC8227E1. 초기 v39 덮어 설치 직후 폰 DB도 같은 해시였다.
- 별도 패키지 io.github.owenyu9292.deliverymaster.qa에서만 시험한다. 실사용 DB를 시험 자료로 넣지 않는다.
- 실제 삼성 키보드에서 출발 입력과 기록 버튼 간격 0px를 발견했다. 기존 경계/히트 검사만으로는 간격 결함을 검출하지 못했다.
- 후속 간격 검사에서 백업의 기록 불러오기, 구역 정정 반영, 누락 도우미 추가도 0px였음을 재현했다. 구역 순서/추가 버튼 간격도 보완한다.
- 사용자가 폰을 장시간 대기시키지 않도록 컴퓨터 검수와 빌드를 먼저 마친 뒤 필요한 실기기 확인만 요청한다.
